packages:
  - apps/*
  - packages/*

allowBuilds:
  esbuild: true
  protobufjs: true

# pnpm 10/11 默认屏蔽依赖的安装期构建脚本，需显式批准。
# allowBuilds 允许安装期构建；onlyBuiltDependencies 继续限制允许构建的依赖范围。
onlyBuiltDependencies:
  - esbuild
  - protobufjs
